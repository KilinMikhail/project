#!/usr/bin/env sh

set -e

gem install bundler

bundle install

dropdb --if-exists premium-parking-dev
dropdb --if-exists premium-parking-test

bin/rails db:create db:structure:load


bin/rails db:environment:set RAILS_ENV=development
mkdir tmp
